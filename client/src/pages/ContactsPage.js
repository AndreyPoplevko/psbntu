import React from 'react';
import NavBar from '../components/NavBar';

const ContactsPage = () => {
    return (
        <div>
            <NavBar/>
        </div>
    );
};

export default ContactsPage;