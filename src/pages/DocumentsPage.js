import React from 'react';
import NavBar from '../components/NavBar';

const DocumentsPage = () => {
    return (
        <div>
            <NavBar/>
        </div>
    );
};

export default DocumentsPage;