import React from 'react';
import NavBar from '../components/NavBar';

const ForMemberPage = () => {
    return (
        <div>
            <NavBar/>
        </div>
    );
};

export default ForMemberPage;